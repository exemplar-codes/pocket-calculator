import React from "react";

import OperatorKey from "./OperatorKey";

const PlusKey = ({className}) => {
  return <OperatorKey symbol="+" height={210} className={className}/>;
};

export default PlusKey;
